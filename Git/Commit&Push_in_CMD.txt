cd C:\Users\Usuario\Documents\... &:: setear el directorio que queremos pushear
git add . 								   &:: actualiza la "foto" del repositorio
git commit -m "Add existing file" 				   &:: el commit con el comentario
git show-ref 							   &:: esto es para ver cuales son las branches del proyecto. Usualmente es "main" o "master"
git push origin main 						   &:: aca main es el nombre de la branch